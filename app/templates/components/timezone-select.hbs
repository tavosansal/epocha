{{log timezoneSelected}}

<PowerSelect @options={{timezones}} @selected={{timezoneSelected}} @onChange={{action "selectionChanged"}} @searchEnabled={{true}} as |timezone|>
  {{timezone}}
</PowerSelect>